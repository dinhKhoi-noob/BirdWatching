import React from "react";

const Hero = () => {
  return (
    <section className="hero">
      <img src={require("../img/hero-img.jpg").default} alt="" />
    </section>
  );
};

export default Hero;
